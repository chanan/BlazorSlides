@if (CurrentSlide is ISlideWithContent)
{
    <SlideContent CurrentSlide="@((ISlideWithContent)CurrentSlide)" CurrentHorizontalIndex="@CurrentHorizontalIndex" CurrentVerticalIndex="@CurrentVerticalIndex" />
}

@if (CurrentSlide is ISlideContainer)
{
    <SlideContainer CurrentSlide="@((ISlideContainer)CurrentSlide)" CurrentHorizontalIndex="@CurrentHorizontalIndex" CurrentVerticalIndex="@CurrentVerticalIndex" />
}

@code {
    [Parameter] public IHorizontalSlide CurrentSlide { get; set; }
    [Parameter] public int CurrentHorizontalIndex { get; set; }
    [Parameter] public int CurrentVerticalIndex { get; set; }
}